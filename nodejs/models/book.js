const mongoose = require('mongoose');

/**
 * Book
 * Schema de notre objet
 * Un livre est composé d'un titre(title), un auteur(author) et une description(description)
 * N'oublier pas d'exporter votre modèle
 * module.exports = votre modèle
 */

